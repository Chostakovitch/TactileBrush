#include "TactileBrush.h"

TactileBrush::TactileBrush(unsigned int lines, unsigned int columns, float distance) {

}

std::vector<ActuatorStep> TactileBrush::computeStroke(Stroke s) {
  return std::vector<ActuatorStep>(0);
}
